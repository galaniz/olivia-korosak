<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{ ctfl.title }} | {{ site.title }}</title>
  {% if process.env.NODE_ENV !== 'production' %}
    <meta name="robots" content="noindex, nofollow">
  {% endif %}
  <meta name="description" content="{{ ctfl.metaDescription if ctfl.metaDescription else site.metaDescription }}">
  <link rel="canonical" href="{{ ctfl.permalink }}">
  <link rel="stylesheet" href="{{ site.styles }}" media="all">
  <link rel="apple-touch-icon" sizes="180x180" href="{{ site.assetsLink }}favicon/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="32x32" href="{{ site.assetsLink }}favicon/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="{{ site.assetsLink }}favicon/favicon-16x16.png">
  <link rel="manifest" href="{{ site.assetsLink }}favicon/site.webmanifest">
  <link rel="mask-icon" href="{{ site.assetsLink }}favicon/safari-pinned-tab.svg" color="{{ site.theme.tint }}">
  <meta name="msapplication-TileColor" content="{{ site.theme.tint }}">
  <meta name="theme-color" content="{{ site.theme.base }}">
  <meta name="format-detection" content="telephone=no">
</head>
<body>
  {{ content | safe }}
</body>
</html>