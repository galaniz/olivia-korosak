# getGradient  

**<code>getGradient(hex: string, type?: &#39;card&#39; | &#39;page&#39;, full?: boolean): string</code>**  

Gradients for page or card.

## Parameters  
- **`hex`** <code>string</code> required  
- **`type`** <code>&#39;card&#39; | &#39;page&#39;</code> optional  
Default: `'card'`  
- **`full`** <code>boolean</code> optional  
Default: `true`

## Returns  

<code>string</code>